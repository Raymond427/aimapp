<%= render 'shared/header'%>
<header>
  <h1>
    Executive Board
  </h1>
  <p>
  AIM&#39;s team consists of highly talented and creative individuals who are passionate about improving the world through teamwork and innovation.
  </p>
</header>
<main>
<!-- <%= select_tag(:current_semester, options_for_select(@executive_semester)) %></br></br>
<%= button_to "Update" %> UNCOMMENT FOR NEW FEATURE-->
<%= button_to "New Executive", new_member_path, :class => "button", :method => :get  %></br>
<% @members.each do |member| %>
  <% full_name = member.first_name + member.last_name %>
  <section class="board_member">
    <figure>
      <img alt= "<%= member.first_name + " " %> <%= member.last_name %>" height="90" src="https://d1q9wbuypc40mm.cloudfront.net/pfv5n8898qr83xj.jpg" width="110" />
      <figcaption>
        <p>
        <b><%= member.executive_position %></b>
        </p>
           <% if member.linkedin_url == nil %>
        <p><%= member.first_name + " " %> <%= member.last_name %></p>
           <% else %>
        <p>
          <% member.linkedin_url %>
        </p>
           <% end %>
        <p>
          <%= member.email %>
        </p>
        <p>
          <%= member.major %>
        </p>
          <% if current_user && current_user.editor? || current_user && current_user.admin? %>
            <!-- <%= link_to 'Show', member_path(member) %> -->
            <%= link_to 'Edit', edit_member_path(member) %>
          <% end %>
          <% if current_user && current_user.admin? %>
            <%= link_to 'Destroy', member, method: :delete, data: { confirm: 'Are you sure you want to delete ' + member.first_name + '?'}%>
          <% end %>
      </figcaption>
    </figure>
  </section>
<% end %>
</main>
